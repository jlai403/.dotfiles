# Linux (Omarchy) Ghostty settings.
# Omarchy's packaged defaults (read-only) load first; personal overrides
# load last (later files win).

config-file = /usr/share/omarchy/config/ghostty/config
config-file = ~/.config/ghostty/overrides.conf
