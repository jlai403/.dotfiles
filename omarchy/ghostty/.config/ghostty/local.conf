# Linux (Omarchy) Ghostty settings.
# Pull Omarchy's packaged defaults (read-only, updates with `omarchy update`),
# then keep only personal deviations below.

config-file = /usr/share/omarchy/config/ghostty/config
font-size = 8
