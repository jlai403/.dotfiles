-- Personal look'n'feel deltas; loaded after all Omarchy defaults.
-- gaps match aerospace (mac): inner/outer both 10. Border 1 = crisp at scale 2.
hl.config({
  general = {
    gaps_in = 10,
    border_size = 1,
  },
})
