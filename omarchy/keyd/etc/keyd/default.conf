# Hyper key: hold CapsLock = Ctrl+Alt+Super+Shift (Karabiner-style Hyper),
# tap CapsLock = Esc.
# Bindings in Hyprland use the full chord, e.g.
#   o.bind("SUPER + SHIFT + CONTROL + ALT + T", "Label", "cmd")
[ids]
*

[main]
capslock = overload(hyper, esc)

[hyper:C-M-S-A]
