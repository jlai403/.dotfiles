# Hyper key: hold CapsLock = Ctrl+Alt+Super (AeroSpace-style Hyper, no Shift),
# tap CapsLock = Esc.
# Bindings in Hyprland use the full chord, e.g.
#   o.bind("SUPER + CONTROL + ALT + T", "Label", "cmd")
[ids]
*

[main]
capslock = overload(hyper, esc)

[hyper:C-M-A]
